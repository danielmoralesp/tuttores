<%= form_for @experience do |f| %>
  <% if @experience.errors.any? %>
    <div id="errors_explanation">
      <h2><%= pluralize(@experience.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% @experience.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="text-center m-b-md" id="wizardControl">

      <a class="btn btn-default" href="#step1" data-toggle="tab">Step 1 - Personal data</a>
      <a class="btn btn-primary" href="#step2" data-toggle="tab">Step 2 - Payment data</a>
      <a class="btn btn-default" href="#step3" data-toggle="tab">Step 3 - Approval</a>

  </div>

  <div class="tab-content">
  <div id="step1" class="p-m tab-pane active">

      <div class="row">
          <div class="col-lg-3 text-center">
              <i class="pe-7s-user fa-5x text-muted"></i>
              <p class="small m-t-md">
                  <strong>Lorem Ipsum</strong> is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard.
                  <br/><br/>Lorem Ipsum has been the industry's dummy text of the printing and typesetting
              </p>
          </div>
          <div class="col-lg-9">
              <div class="row">

                <div class="form-group col-lg-12">
                  <%= f.label :cargo, 'Cargo:' %>
                  <%= f.text_field :cargo, required: true, class: 'form-control', placeholder: "Ingrese el cargo desempeñado" %>
                </div>

                <div class="form-group col-lg-12">
                  <%= f.label :empresa, 'Empresa:' %>
                  <%= f.text_field :empresa, required: true, class: 'form-control', placeholder: "Ingrese el nombre de la empresa" %>
                </div>

              </div>
          </div>
      </div>

      <div class="text-right m-t-xs">
        <%= f.hidden_field :user_id, value: current_user.id %>
        <%= f.hidden_field :teacher_id, value: "#{value}" %> <!-- new = @teacher.id, edit= @teacher -->
        <%= link_to 'Volver', teachers_path, class: 'btn btn-danger' %>
        <%= f.submit "#{action} Experiencia", class: 'btn btn-success' %>
      </div>

  </div>

</div>
<% end %>
